using System.Collections;
using System.Collections.Generic;
using UnityEngine;

public class zahyouTest : MonoBehaviour {

    [SerializeField]
    GameObject object1;

	// Use this for initialization
	void Start () {
		
	}
	
	// Update is called once per frame
	void Update () {
        //変換後座標l = Camera.main.WorldToScreenPoint(変換したい座標);
    }
}
